
    // i dont know this yet

    // function meetTwoRule(pwd) {
    //     let count = 0;
    //     if (/[A-Z]/.test(pwd)) count++;   // has uppercase
    //     if (/[a-z]/.test(pwd)) count++;   // has lowercase
    //     if (/\d/.test(pwd)) count++;   // has digit
    //     if (/[^A-Za-z0-9]/.test(pwd)) count++; // special char
    //     return count >= 2;
    // }

    // function meetAllRule(pwd) {
    //     return (
    //         /[A-Z]/.test(pwd) &&
    //         /[a-z]/.test(pwd) &&
    //         /\d/.test(pwd) &&
    //         /[^A-Za-z0-9]/.test(pwd)
    //     );
    // }